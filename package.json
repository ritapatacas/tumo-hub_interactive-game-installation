{
  "name": "tumo-hub-interativo-base",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:host:sync": "node server/dev-host-sync.mjs",
    "sync": "node server/sync-server.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
